module.exports = {
    // Purge will come at later stage when the final builder output is compiled
    // purge: [
    //     './src/scss/**/.scss',
    //     './src/ts/**/.ts'
    // ],
    darkMode: false,
    theme: {
        extend: {}
    },
    variants: {
        extend: {}
    },
    plugins: []
};
